# 多图片功能开发进度汇报 (2026-01-20)

## 1. 核心状态管理 (已完成)
- [x] **ProjectFileSystem**: 实现了基于 IndexedDB 的文件存储适配器，支持存取原图、缩略图和项目元数据。
- [x] **useProjectState**: 重构为符合架构规范的 Composable，支持多项目管理（增删改查、批量导入）。
- [x] **状态桥接**: 实现了 `useTextureState` 与 `projectState` 的联动，确保 `activeOriginalDataUrl` 的变化能实时驱动编辑器更新。
- [x] **参数同步**: `useAdjustmentParams` 已实现从项目状态的双向同步（自动保存参数修改）。

## 2. 界面集成 (已完成)
- [x] **桌面端布局**: 
    - `Desktop.vue` 改造为三栏布局 (左侧控制、中间视图、右侧项目列表)。
    - 集成了 `ProjectSidebar` 组件。
- [x] **移动端适配**:
    - 新增 `ProjectDrawer` 组件，支持底部弹出式项目管理。
    - `App.vue` 集成了 `ProjectDrawer`。
- [x] **拖拽上传**:
    - `useGlobalDragDrop` 升级支持多文件拖拽，自动创建项目并切换。
- [x] **缩略图管理**:
    - 抽离了 `useProjectThumbnails` 逻辑，实现了缩略图的懒加载与缓存管理。

## 3. 批量导出功能 (已完成/待验证)
- [x] **核心逻辑**:
    - 实现了 `BatchExporter` 类 (单例/Composable)，支持队列管理。
    - 实现了 `processImageToTileable` 管线调用，支持应用项目参数。
    - 集成 `jszip` 支持打包下载。
    - 支持格式转换 (WebP/PNG/JPEG)、尺寸调整、命名模板。
- [x] **UI 组件**:
    - `ExportDialog`: 导出配置弹窗，包含预设选择、模式切换 (逐个/打包)、自定义设置。
    - `ExportProgress`: 导出进度面板，显示进度条、剩余时间、任务列表，支持取消和重试。
- [x] **集成**:
    - `Desktop.vue` 已集成导出弹窗和进度条组件。
    - 导出逻辑已对接 `useBatchExport`。

## 4. 依赖项
- [x] 安装 `jszip` 及 `@types/jszip`。
- [x] 更新 `imports.ts` 统一管理外部依赖。

## 5. 待办 / 下一步计划
- [ ] **功能验证**: 需测试完整的批量导出流程，特别是这几种情况：
    - 大量图片导出时的内存占用。
    - ZIP 打包模式的稳定性。
    - 各种图片格式和尺寸调整的正确性。
- [ ] **水印配置**: 目前点击由导出弹窗触发的水印配置仅弹出提示，需要对接实际的水印配置面板打开逻辑。
- [x] **错误处理**: 完善导出失败时的用户反馈和重试机制细节 (已在 `useBatchExport` 中实现基础错误捕获与重试)。
- [x] **代码清理**: 检查 `Desktop.vue` 和其他组件中是否存在未使用的导入或冗余代码 (已核查无冗余)。

## 6. 已知问题
- [x] **持久化失效**: 图片列表在页面刷新后为空，似乎没有正确从 IndexedDB 加载。已修复：
    - 在 `useProjectState` 中添加了自动初始化逻辑。
    - 在 `loadProjects` 后添加了自动选中最新项目的逻辑。
- [x] **预览同步问题**: 切换图片项目后，右侧查看器 (Viewer) 有时未正确更新为新图片的预览。
    - 原因：`Desktop.vue` 中的 `watch` 与 `useTextureState` 中的 `computed` 冲突，导致切换时错误地触发了“新建项目”逻辑。
    - 修复：删除了 `Desktop.vue` 中多余的 `watch`，由 `useTextureState` 内部的响应式机制自动处理。
- [x] **批量应用功能缺失**: 
    - 实现：在项目侧边栏实现了右键菜单，支持“应用到所有/上方/下方项目”及删除功能。
- [x] **参数持久化异常**: 切换到其它图片再切换回来之后，并没有正确加载已经应用并保存的参数(UI未更新)。
    - 原因：各个调整面板组件 (`ExposurePanel`, `DehazePanel` 等) 监听了 `originalImage` 的变化并重置为默认值，覆盖了从 `props` 传入的正确参数。
    - 修复：移除了各个面板中对 `originalImage` 的重置监听，改用 `watch` 监听各个参数 `props` 的变化并同步到本地状态。
- [x] **批量应用无效**: 
    - 即使逻辑执行但结果不生效，是因为参数持久化异常导致项目状态被重置为默认值。
    - 修复：上述参数持久化异常修复后，批量应用功能应当恢复正常，因为项目状态现在能正确反映用户修改。
- [ ] **左右侧状态再次不一致**: (2026-01-20 新增)
    - 现象：用户反馈左右侧状态再次出现不一致。从截图看，左侧原图显示为卧室场景，右侧平铺预览也是卧室场景，但项目列表选中的似乎是另一个项目（或预览未及时刷新）。或者左侧显示的图与右侧生成的纹理不匹配。具体表现为：项目列表选中项、左侧原图预览、右侧无缝结果三者之间存在同步脱节。
    - 疑似原因：可能仍存在某些状态更新的竞态条件，或者 `useTextureState` 对 `projectState` 的响应存在漏洞。需要进一步排查。

## 7. 备注
- **内存优化注意**: 目前批量导出 (ZIP模式) 会将所有结果保存在内存中再打包，对于大量高分辨率图片可能导致 OOM。建议后续增加分批打包或流式下载 (需 ServiceWorker 支持)。
- **下一步**: 进行完整的端到端测试，特别是验证 IndexedDB 在大数据量下的读写性能。
