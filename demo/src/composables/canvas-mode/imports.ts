/**
 * 画布模式管理 - 导入转发
 *
 * 仅用于转发外部依赖
 */

// Vue 导入
import { ref, computed, shallowRef, watch } from 'vue'
import type { Ref, ComputedRef } from 'vue'

export { ref, computed, shallowRef, watch }
export type { Ref, ComputedRef }
