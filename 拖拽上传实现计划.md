# 拖拽上传实现计划

## 目标
目前 `Desktop.vue` 仅支持通过 `DesktopControls` 组件内部的按钮点击上传。为了提升用户体验，需在桌面端实现全屏拖拽上传功能。

## 涉及文件
- `d:\dev\seamless-texture-generator\demo\src\Desktop.vue`

## 实现步骤

### 1. 状态与UI准备 (Desktop.vue)
- [x] **添加状态**: 新增 `isDragging` (boolean) 响应式变量，用于控制拖拽覆盖层的显示。
- [x] **添加拖拽计数器**: 新增 `dragCounter` (number) 变量，用于解决 `dragleave` 在子元素上触发导致覆盖层闪烁的问题。
- [x] **添加覆盖层UI**: 
    - 在根 `div` 内部添加一个绝对定位 (`absolute inset-0 z-50`) 的覆盖层。
    - 样式建议：半透明黑色背景 (`bg-black/60`)，虚线边框，居中显示 "释放图片以导入" 或类似提示。
    - 仅当 `isDragging` 为 `true` 时显示。

### 2. 事件监听逻辑 (Desktop.vue)
- [x] **绑定事件**: 在根 `div` (`w-screen h-screen`) 上绑定以下事件：
    - `@dragenter.prevent="handleDragEnter"`
    - `@dragleave.prevent="handleDragLeave"`
    - `@dragover.prevent` (阻止默认行为以允许 drop)
    - `@drop.prevent="handleDrop"`
- [x] **实现 handleDragEnter**:
    - `dragCounter++`
    - `if (dragCounter === 1) isDragging = true`
- [x] **实现 handleDragLeave**:
    - `dragCounter--`
    - `if (dragCounter === 0) isDragging = false`

### 3. 文件处理逻辑 (Desktop.vue)
- [x] **实现 handleDrop**:
    - 重置状态: `dragCounter = 0`, `isDragging = false`
    - 获取文件: `const file = event.dataTransfer?.files[0]`
    - **验证**: 检查 `file` 是否存在以及 `file.type.startsWith('image/')`。若非图片，提示错误 (可调用 `handleControlEvent` 发送错误信息，或者直接 alert/console 警告)。
    - **读取**: 使用 `FileReader` 读取文件内容 (`readAsDataURL`)。
    - **触发更新**: 读取成功后，调用 `handleControlEvent`:
      ```typescript
      handleControlEvent({
        type: 'update-data', // EVENT_TYPE.UPDATE_DATA
        detail: {
          action: 'set-image', // DATA_ACTION.SET_IMAGE
          data: result // base64 string
        }
      })
      ```

### 4. 常量引用
- [x] 确保正确引用事件常量 (如需)：
    - `EVENT_TYPE.UPDATE_DATA` ('update-data')
    - `DATA_ACTION.SET_IMAGE` ('set-image')
    - 也可以直接使用字符串字面量，保持与 `controlEventHandler` 一致即可。

### 5. 代码重构 (Lint修复)
- [x] **提取逻辑**: 将拖拽逻辑提取到 `composables/useGlobalDragDrop.ts`，以避免 `Desktop.vue` 脚本过长 (UI-002)。
- [x] **复用性**: 确保 Composable 接收 `onSuccess` 回调，保持通用性。

## 验证计划
1. 拖拽一张图片到窗口任意位置，应出现覆盖层。
2. 释放图片，应自动替换当前显示的图片（Source & Viewer）。
3. 拖拽非图片文件，应无响应或提示错误，且覆盖层消失。
4. 拖拽进入后移出窗口，覆盖层应正确消失。